

git init -> .git/
git remote add name url
git pull
git status
git add <file_name> -> adds <file_name>
	git add . -> adds everything in directory
git commit -m 'message, comment' -> committing changes
git branch -> listing branches
	git branch <branch_name> -> creating new branch named as <branch_name>
git checkout <branch_name> -> switching to <branch_name> if it branch exist, otherwise it creates and switches
git push <branch_name> -> pushes new commits to remote repository
git reset <file_name> -> removes file_name from index(staging area)

